<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CaLcuLator</title>
</head>
<body>
    <div class="bg-white shadow-lg rounded-3xl p-4 sm:p-12 max-w-3xl w-full flex flex-col min-h-0 h-[calc(100dvh-4rem)] m-10">
        <h1 class="text-3xl sm:text-4xl font-bold mb-8 text-center">Todo List</h1>

        <form
          class="mb-6 flex flex-row sm:flex-row items-center border border-gray-300 focus:outline-none focus:ring-black-500 rounded-3xl"
          
          
        >
          <input
            type="text"
            placeholder="Thêm việc cần làm..."
            class="flex-4 min-w-0 w-full border-none px-4 py-3 focus:outline-none"
            

            
          />
          <div class="relative flex items-center">
            <button
              type="button"
              
              aria-label="Chọn thời gian">
            </button>


            <!-- {timeisOpen && (
              <div class="absolute top-full mt-2 left-1/2 -translate-x-1/2 bg-white border border-gray-300 rounded-md p-2 z-10 ">
                <input
                  type="date"
                  class="border border-gray-300 rounded-md p-2"
                  placeholder="Vui lòng chọn ngày"
                  onKeyDown={(e) => {
                    if (e.key === 'Enter') e.preventDefault();
                  }}
                />
                <input
                  type="time"
                  class="border border-gray-300 rounded-md p-2"
                  placeholder="Vui lòng chọn giờ"
                  onKeyDown={(e) => {
                    if (e.key === 'Enter') e.preventDefault();
                  }}
                />


                
              </div>
            )} -->
          </div>
          <button
            type="submit"
            class="w-full flex-1 sm:w-auto bg-black text-white py-3 px-6 rounded-r-3xl hover:bg-gray-900 cursor-pointer transition whitespace-nowrap"
          >
            Thêm
          </button>

        </form>

        <div class="flex flex-col min-h-0 flex-1">
          
        </div>
      </div>
</body>
</html>